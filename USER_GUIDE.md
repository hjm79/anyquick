# AnyQuick 사용자 사용설명서

## 1. 앱 소개
AnyQuick은 자연어 입력 한 줄로 다음 작업을 빠르게 실행하는 iOS 앱입니다.

- 연락처 기반 메시지 발송
- 현재 위치 링크 공유(카카오맵/네이버맵)
- 일정 등록(캘린더) 또는 미리알림 등록
- 길안내 실행
- 웹 검색 실행(Naver, Google, YouTube, TMDB, AppStore)
- 사전 검색(영어사전, 국어사전, 한자사전) — 인앱 사파리로 열림
- AI 질문 ("요약해줘", "번역해줘", "설명해줘" 등)
- 앱 바로가기 (등록한 앱을 숏컷으로 바로 실행)

---

## 2. 빠른 시작
1. 앱을 실행합니다.
2. 상단 입력창(`명령어나 검색어를 입력하세요...`)에 문장을 입력합니다.
3. 아래 예측 카드(예: `메시지 발송`, `일정 등록`, `길안내 시작`)를 탭하거나 키보드 `Go`를 누릅니다.
4. 기능에 따라 선택창이 나타나면 원하는 방식을 고릅니다.

---

## 3. 화면 구성

### 3.1 상단 입력 영역
- `TextField`: 명령/검색어 입력
- `mic.fill` 버튼: 마이크 받아쓰기 (입력 비어있고 키보드 내려간 상태에서 표시)
- `doc.on.clipboard.fill` 버튼: 클립보드 붙여넣기
- `xmark` 버튼: 입력 내용 지우기 + 키보드 닫기 (텍스트 있거나 키보드 활성 시 표시)

### 3.2 예측 카드
입력 내용에 따라 아래 카드가 자동으로 바뀝니다.
- `연락처 선택`
- `일정 등록`
- `메시지 발송`
- `길안내 시작`
- `웹 검색`

### 3.3 하단 숏컷 버튼 (3열 그리드)
입력 텍스트가 있을 때 카테고리별 검색 숏컷을 사용할 수 있습니다.

| 카테고리 | 하위 서비스 |
|----------|------------|
| 웹검색 | Naver, Google, YouTube, TMDB, AppStore |
| 쇼핑 | 네이버쇼핑, Coupang, AliExpress |
| AI | ChatGPT, Gemini, Claude, Perplexity, Grok |
| 지도 | NaverMap, KakaoMap, 카카오길안내, Tmap, 네이버 경로\*, 카카오 경로\* |
| 사전 | 영어사전, 국어사전, 한자사전 |
| 미리알림 | 입력된 텍스트를 미리알림으로 등록 |
| 앱열기 | 등록한 앱 바로 실행 + 앱 추가 |

`위치`, `현재 위치`, `내 위치` 등의 키워드가 입력되면 `위치공유` 버튼이 추가로 나타납니다.

---

## 4. 주요 기능 사용법

## 4.1 메시지 / 전화 / 이메일
입력 예시:
- `김철수에게 안부 보내줘` — 문자/공유 선택 시트
- `엄마한테 회의 10분 늦을 것 같아` — 문자/공유 선택 시트
- `정만에게 현재 위치 보내줘` — 위치 링크 포함 메시지
- `엄마한테 전화해` — **바로 전화 연결** (시트 스킵)
- `정만에게 이메일` — **바로 이메일 작성** (시트 스킵)

동작 방식:
1. 앱이 연락처 이름을 찾아 수신 대상을 인식합니다.
2. "전화해/이메일" 키워드가 있으면 바로 실행합니다.
3. 키워드가 없으면 전송 방식 선택창이 열립니다.
   - 📞 **전화 걸기**
   - `문자로 보내기`
   - 📧 **이메일 보내기** (연락처에 이메일이 있을 때만 표시)
   - `앱 선택해서 공유하기` (카카오톡 포함)

참고:
- 동명이인/유사 이름일 때 `연락처 선택` 시트가 먼저 열릴 수 있습니다.
- 기기에서 문자 기능을 사용할 수 없으면 앱 공유 방식으로 자동 전환됩니다.

## 4.2 현재 위치 공유
입력 예시:
- `현재위치 카톡 보내기`
- `위치 메시지 보내줘`
- `정만에게 위치 카톡 보내기`

동작 방식:
1. 전송 방식(`문자` 또는 `앱 공유`)을 선택합니다.
2. 메시지 본문에 아래 정보가 자동 추가됩니다.
   - 카카오맵 위치 URL
   - 네이버맵 앱 URL(`nmap://...`)
   - 네이버맵 웹 URL
   - 위도/경도 좌표

참고:
- 위치 권한이 없으면 위치 링크 없이 텍스트만 전송됩니다.
- 수신자가 네이버맵 앱이 없으면 네이버 웹 링크를 사용할 수 있습니다.

## 4.3 일정/미리알림 등록
입력 예시:
- `내일 오후 3시에 회의`
- `다음주 화요일 오후 3시 반에 강남에서 팀 미팅`
- `오늘 휴가`

동작 방식:
1. 앱이 날짜/시간/장소/제목을 파싱합니다.
2. `등록 방식을 선택하세요` 다이얼로그가 열립니다.
   - `일정으로 등록`: 캘린더 편집 화면(EKEventEditViewController) 열기
   - `미리알림으로 등록`: 기본 미리알림 목록에 저장

참고:
- 시간 없이 입력하면 종일 일정으로 처리됩니다.
- `...에서` 구문은 위치로 저장됩니다. 예: `강남에서`

## 4.4 길안내
입력 예시:
- `강남역 길찾기` — 단일 목적지
- `판교 카카오맵` — 단일 목적지
- `광화문에서 신창시장` — A→B 경로 (자동 감지)
- `서울역부터 인천공항` — A→B 경로 (자동 감지)
- `광화문 신창시장` + **네이버 경로** 버튼 — 수동 경로 숙컷

동작 방식:
1. **단일 목적지**: 네이버맵/카카오맵/카카오내비/티맵 중 선택
2. **A→B 경로**: 네이버맵/카카오맵만 선택 가능
3. 앱이 장소명을 좌표로 변환 후 경로 안내 실행

참고:
- **경로 숙컷**: 입력에 공백이 있으면 지도 카테고리에 "네이버 경로" / "카카오 경로" 버튼이 추가로 표시됩니다
- 좌표 변환 실패 시 일반 검색으로 자동 전환됩니다

## 4.5 웹 검색
입력 예시:
- `아이폰 17 검색`
- `뉴진스 유튜브`
- `기생충 영화`
- `영한 사전 apple`

동작 방식:
- 입력 키워드와 트리거 단어를 조합해 해당 서비스 검색을 바로 실행합니다.
- **사전 검색은 인앱 사파리(SFSafariViewController)로 열립니다.**
- **키워드만 입력 시**: 해당 앱을 직접 실행합니다. (예: `유튜브` → YouTube 앱 열기)

## 4.6 AI 질문
입력 예시:
- `스위프트 요약해줘`
- `이 문장 번역해줘`
- `양자역학 설명해줘`
- `날씨 물어봐` / `날씨 물어바` (오타도 인식)

동작 방식:
- 기본 AI 서비스가 설정되어 있으면 해당 서비스로 바로 실행
- 미설정 시 AI 서비스 선택 시트 표시
- `chatgpt`, `grok` 등 명시 키워드는 해당 서비스로 직접 실행

## 4.7 앱 바로가기
등록한 앱을 숏컷 그리드에서 바로 실행할 수 있습니다.

등록 방법:
1. 숏컷 그리드에서 `앱열기` 카테고리 탭
2. `앱 추가` 버튼 탭
3. 앱 이름, URL Scheme, 아이콘 선택

사용 예시:
- 카카오톡: `kakaotalk://`
- 인스타그램: `instagram://`
- 네이버: `naversearchapp://`

환경설정 → `앱 바로가기` 섹션에서도 관리(추가/삭제) 가능합니다.

---

## 5. 환경설정

경로:
1. 입력창 오른쪽 `설정(gear)` 버튼 탭
2. `환경설정` > `일정 등록 캘린더`
3. 아래 중 선택
   - `기본 캘린더 사용`
   - 특정 캘린더(수정 가능한 캘린더 목록)

설정한 캘린더는 이후 `일정으로 등록` 시 기본값으로 적용됩니다.

---

## 6. 권한 안내
앱 기능 사용을 위해 아래 권한이 필요합니다.

- 연락처(`Contacts`): 이름 인식 및 수신자 전화번호 조회
- 위치(`Location When In Use`): 현재 위치 링크 생성
- 캘린더(`Calendars`): 일정 등록
- 미리알림(`Reminders`): 미리알림 등록

권한을 거부한 경우:
- 해당 기능이 제한되며, 필요한 경우 iOS 설정에서 다시 허용해야 합니다.

---

## 7. 문제 해결

### 7.1 연락처 이름이 인식되지 않을 때
- 연락처 권한 허용 여부를 확인합니다.
- 이름 표기를 간단히 바꿔 다시 입력합니다. (예: `김철수에게 ...`)

### 7.2 위치 링크가 메시지에 붙지 않을 때
- 위치 권한 허용 여부를 확인합니다.
- 실내/권한 제한 상태에서는 좌표 획득이 실패할 수 있습니다.

### 7.3 일정/미리알림 등록이 되지 않을 때
- 캘린더/미리알림 권한 허용 여부를 확인합니다.
- 일정 저장 대상 캘린더가 수정 가능한 상태인지 확인합니다.

### 7.4 네이버맵 앱 링크가 바로 열리지 않을 때
- 수신 기기에 네이버맵 앱 설치 여부를 확인합니다.
- 설치되지 않은 경우, 함께 제공되는 네이버맵 웹 링크를 사용합니다.

---

## 8. 사용 팁
- 수신자를 먼저 명확히 적으면 인식률이 올라갑니다. 예: `홍길동에게 ...`
- 위치 공유가 목적이면 `현재위치 카톡 보내기`처럼 채널(카톡/문자)과 동사(보내기/공유/전송)를 함께 쓰면 정확합니다.
- 일정 문장은 날짜/시간을 앞쪽에 두면 파싱이 안정적입니다. 예: `내일 오전 10시에 ...`
- 앱 이름만 입력하면 해당 앱이 바로 열립니다. 예: `유튜브` → YouTube 앱 실행
- 맞춤법이 조금 틀려도 인식됩니다. 예: `물어바`, `알려조`, `보내조`
